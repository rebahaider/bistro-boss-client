

const Button = ({buttonName}) => {
    return (
        <button className="btn btn-outline btn-warning font-extrabold border-b-4 border-0 mt-4">{buttonName}</button>
    );
};

export default Button;